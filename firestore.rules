rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public car metadata
    match /cars/{carId} {
      allow read: if true;
      allow write: if false;
    }

    // Add more collections later (comments, etc) with proper auth checks
  }
}